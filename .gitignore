bin/
obj/
*.user
.DS_Store

# Build output staged for Thunderstore
thunderstore/build/

# Automatic test output (scripts/autotest.sh): log, screenshots, test saves
.devtest/

# .claude/ is tracked (the rules' reasons, research, plans, handoffs, the skill), so a
# friend working on the mod sees them. Only personal settings stay local.
.claude/settings.local.json
